;test file
(define 
    (fib i)
    (if (< i 2)
        i
        (+ 
            (fib (- i 1))
            (fib (- i 2))
)))

(define 
    (sum lst)
    (if (null? lst)
        0 
        (+ 
            (car lst)
            (sum (cdr lst)))))

(define 
    (times i)
    (if (< i 0)
        (list)
        (cons 
            i 
            (times (- i 1))
)))

(define (test b) 
    (if b (list 1) (list 2))
)

;(display (test #f))

(display (sum (times 5)) )

;(display (cons 3 (list )))

;(display (sum (list 1 2 3 4 5)))