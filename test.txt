;test file
(define 
    (fib i)
    (if (< i 2)
        i
        (+ 
            (fib (- i 1))
            (fib (- i 2))
)))

(define 
    (sum lst)
    (if (null? lst)
        0 
        (+ 
            (car lst)
            (sum (cdr lst)))))

(display (sum (list 1 2 3 4 5)))