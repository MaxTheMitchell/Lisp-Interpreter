(define (double x) (* 2 x) )

(define (quadruple x) (double (double x) ) )

(define (add x y) (+ x y))

(define foo 7)

(display 
    (quadruple 
        (display 
            (quadruple 
                foo
))))

(display (add 2 3) )